server:
  servlet:
    context-path: /session10Boot
spring:
  datasource: 
    url: jdbc:mysql://localhost/Employeeportal
    username: root
    password: 1234
    driver-class-name : com.mysql.jdbc.Driver
    type: org.apache.tomcat.jdbc.pool.DataSource
  mail:
    default-encoding: UTF-8
    host: smtp.gmail.com
    username: 
    password: 
    port: 587
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
    protocol: smtp
    test-connection: false
