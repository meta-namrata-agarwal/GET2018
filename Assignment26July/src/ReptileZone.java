public class ReptileZone extends Zone{

    /**
     * It initializes the ReptileZone properties into Zoo
     * @param capacity
     * */
    public ReptileZone(int capacity) {
        
        super(AnimalCategory.REPTILE, capacity);
    }
}